{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC capability for the koi tray popover window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
